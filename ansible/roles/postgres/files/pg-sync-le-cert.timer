# Managed by Ansible (roles/postgres)
[Unit]
Description=Daily backstop for the Postgres Let's Encrypt certificate sync

[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
