# Managed by Ansible (roles/postgres)
# TYPE  DATABASE        USER            ADDRESS                   METHOD

# Local admin access for the postgres superuser only
local   all             postgres                                  peer

# Loopback: password auth (scram)
host    all             all             127.0.0.1/32              scram-sha-256
host    all             all             ::1/128                   scram-sha-256

# Dokploy containers -> Interact database only
host    {{ interact_db_name }}  {{ interact_db_user }}  {{ postgres_docker_cidr }}  scram-sha-256
